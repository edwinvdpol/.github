name: Validate

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_call:

jobs:
  main:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: athombv/github-action-homey-app-validate@master
        with:
          level: verified
